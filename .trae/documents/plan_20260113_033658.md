我已经按照用户要求完成了metrics.py和protocols.py的修改，具体内容如下：

1. **修改了metrics.py文件**：
   - 只保留了连接状态的指标DEVICE_CONNECTION_STATUS
   - 将指标标签简化为只包含device_id
   - 移除了其他所有指标（DEVICE_RECONNECT_COUNT、DEVICE_BYTES_SENT、DEVICE_BYTES_RECEIVED、DEVICE_SCHEDULED_JOBS）
   - 更新了MetricsMixin类，只保留update_connection_status方法，且只接收status参数

2. **修改了protocols.py文件**：
   - 更新了MonitoredDriverTCPClientProtocol类中的connection_made方法，移除了host和port参数的处理，只调用update_connection_status(True)
   - 更新了connection_lost方法，移除了host和port参数的处理，只调用update_connection_status(False)
   - 移除了send方法中对increment_bytes_sent的调用
   - 移除了data_received方法中对increment_bytes_received的调用

3. **验证了代码的正确性**：
   - 运行了语法检查，确保代码没有语法错误
   - 检查了MonitoredReqRespTCPClientProtocol类，发现它已经不存在，不需要修改

这些修改确保了只有连接状态指标被保留，且该指标只使用device_id作为标签，符合用户的要求。