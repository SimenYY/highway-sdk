1. **修改指标标签名称**：
   - 将`metrics.py`中的`connection_id`标签改回`device_id`，使其更通用
   - 更新指标描述，明确表示适用于所有设备类型

2. **调整MetricsMixin**：
   - 将`MetricsMixin`中的`connection_id`改回`device_id`
   - 确保设备ID生成逻辑能够处理不同协议类型

3. **修改连接器中的设备ID生成**：
   - 在`BaseConnector`中添加`device_id`属性，根据协议类型生成通用设备ID
   - 为TCP生成`TCP_{host}_{port}`格式的ID
   - 为UDP生成`UDP_{host}_{port}`格式的ID

4. **更新协议类**：
   - 将`MonitoredDriverTCPClientProtocol`中的`connection_id`改回`device_id`
   - 确保所有协议类都能接受和使用`device_id`

5. **保持connection_status指标名称**：
   - 保留`connection_status`指标名称，因为它准确反映了设备的连接状态
   - 但确保该指标适用于所有协议类型，包括无连接的UDP

这些修改将确保：
- 设备ID命名更通用，适合所有协议类型
- connection_status指标能够正确反映所有设备的在线状态
- 设备ID包含协议类型、主机和端口信息，便于监控和识别
- 代码保持一致性和可维护性