1. **合并类定义**：
   - 将LoguruUtils类的功能合并到LoguruConfig类中
   - 移除LoguruUtils类，只保留LoguruConfig类
   - 保持LoguruConfig作为唯一的日志配置入口

2. **合并功能**：
   - 将LoguruUtils.DEFAULT_FORMAT常量移动到LoguruConfig中
   - 将LoguruUtils.intercept_logging方法合并为LoguruConfig的类方法
   - 将LoguruUtils.propagate_logging方法合并为LoguruConfig的实例方法
   - 将LoguruUtils.reset_logging方法与LoguruConfig.clear_all_intercepts方法合并

3. **优化代码结构**：
   - 统一日志拦截和配置的接口
   - 保持API的向后兼容性
   - 简化用户使用方式

4. **更新文档**：
   - 更新类文档字符串，包含所有功能说明
   - 更新示例代码，展示合并后的使用方式

5. **移除重复代码**：
   - 删除LoguruUtils类的定义
   - 确保所有功能都通过LoguruConfig类提供

合并后的LoguruConfig类将提供完整的日志配置功能，包括拦截规则设置、控制台和文件输出配置、日志格式定义等，减少代码重复，提高可维护性。